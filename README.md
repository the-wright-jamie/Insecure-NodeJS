# Insecure-NodeJS

An insecure NodeJS 'API' server written for experiments and demonstrations

Required env values:

```
HOST_PORT=
DOMAIN=
ENVIRONMENT=
PGUSER=
PGHOST=
PGPASSWORD=
PGDATABASE=
PGPORT=
```
